name: CI
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install modules
        run: yarn
      - name: Run tests
        run: yarn test:ci
      - name: Jest badges generation action
        uses: jpb06/jest-badges-action@v1.8.8